var base = window.prompt("Enter the base triangle", "");
var height = window.prompt("Enter the height of triangle", "");

console.log(`The Base of triangle is ${base}`);
console.log(`The Height of triangle is ${height}`);

 var areaOfTriangle = (base*height)/2;
console.log(`The Area of triangle is ${areaOfTriangle}`)